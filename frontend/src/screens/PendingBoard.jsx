import React, { useState } from 'react'
import { Card, PageHead, Badge } from '../components.jsx'
import { SIGUN } from '../data.js'
export default function PendingBoard({data,addPending}){const [form,setForm]=useState({name:'',vehicleNo:'',sigun:'춘천시',memberType:'택배',membership:'협회미가입',certIssueDate:'2026-06-10',note:''}); return <div><PageHead title="신규 · 예정자" desc="자격증명 발급 → 시청 신규허가 서류 → 예정자 등록 → 전체자명단 전환 흐름입니다."/><div className="grid grid-2"><Card className="card-pad"><h3>예정자 등록</h3><div className="form-row"><b>이름</b><input className="input" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div className="form-row"><b>차량번호</b><input className="input" value={form.vehicleNo} onChange={e=>setForm({...form,vehicleNo:e.target.value})}/></div><div className="form-row"><b>지역</b><select className="select" value={form.sigun} onChange={e=>setForm({...form,sigun:e.target.value})}>{SIGUN.map(s=><option key={s}>{s}</option>)}</select></div><div className="form-row"><b>구분</b><select className="select" value={form.memberType} onChange={e=>setForm({...form,memberType:e.target.value})}><option>개인</option><option>택배</option></select></div><div className="form-row"><b>가입여부</b><select className="select" value={form.membership} onChange={e=>setForm({...form,membership:e.target.value})}><option>협회가입</option><option>협회미가입</option></select></div><div className="form-row"><b>발급일자</b><input className="input" type="date" value={form.certIssueDate} onChange={e=>setForm({...form,certIssueDate:e.target.value})}/></div><div className="notice">관리비는 자격증명 발급일자 다음 달부터, 협회비는 협회 가입일자 다음 달부터 부과됩니다.</div><button className="btn primary" style={{marginTop:12}} onClick={()=>{if(form.name&&form.vehicleNo)addPending(form)}}>예정자 추가</button></Card><Card><div className="table-wrap"><table className="table" style={{minWidth:760}}><thead><tr><th>이름</th><th>차량번호</th><th>지역</th><th>구분</th><th>가입</th><th>발급일</th><th>단계</th></tr></thead><tbody>{data.pending.map(p=><tr key={p.id}><td><b>{p.name}</b></td><td className="mono">{p.vehicleNo}</td><td>{p.sigun}</td><td>{p.memberType}</td><td>{p.membership}</td><td>{p.certIssueDate}</td><td><Badge tone="blue">{p.step}</Badge></td></tr>)}</tbody></table></div></Card></div></div>}
